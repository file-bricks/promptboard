# TASKPLAN-Readback — PromptBoard — stale selector / foreign projection

Zeitpunkt: 2026-08-13 00:01 Europe/Berlin  
Rolle: TASKSOLVER  
Bündel: 1136 / 1137

## Selektorgrenze und TASKPLAN-Status

Der Selektor lieferte exakt:

`C:\Users\lukas\OneDrive\.TOPICS\.SOFTWARE\LLM\REL-PUB_PromptBoard`

Dieser Pfad existiert nicht. Die beiden Datensätze bleiben unverändert:

- 1136: `open`, `assigned_to=tasksolver-codex`,
  `delegation_status=blocked_foreign_state`.
- 1137: `open`, `assigned_to=tasksolver-codex`,
  `delegation_status=blocked_foreign_state`.

Der Selektorpfad wurde nicht durch einen selbst gewählten Nachbarn ersetzt.

## Reale OneDrive-Projektion (Read-only)

Die auffindbare Projektion liegt unter
`C:\Users\lukas\OneDrive\.TOPICS\.SOFTWARE\DATA\REL-PUB_PromptBoard`.
Sie ist nicht als Arbeitsziel übernommen worden:

- `cldflt.sys` ist aktiv; das Cloud-/Rename-Risiko ist hoch.
- Git: `main...origin/main [behind 15]`.
- Umfangreiche getrackte Änderungen, Löschungen im historischen
  `flutter_port/`, ungetrackte Assets, Lizenzdatei und Testflächen sind
  vorhanden.
- `releases/v1.1.1/` enthält sowohl `SHA256SUMS` als auch `SHA256SUMS.txt`.
  Der aktuelle EXE-Hash ist `70dc711f…26159` und stimmt mit `.txt` überein;
  die parallele Datei nennt dagegen `477c2f5c…e2e3`.
- Das aktuelle Source-ZIP hat 41 Einträge, darunter
  `src/__pycache__/*.pyc`; damit ist die Projektion kein eindeutiger sauberer
  Release-Satz.

Keine OneDrive-Datei wurde geändert, bereinigt, überschrieben oder gelöscht.

## Lokaler Plan-D-Readback (nicht als OneDrive-Ersatz)

Der dedizierte Klon
`C:\_Local_DEV\repos\REL-PUB_PromptBoard-tasksolver-1136-1137` ist sauber:
`main...origin/main [ahead 3]`, HEAD `3e29a8dcf7651da4a6b29ba3c3459fefe27fef66`;
der Produktstand liegt auf `origin/main` `be5bcd3`.

Fresh read-only checks im Klon:

- `python -X utf8 -m pytest -q`: **116 passed** (18,38 s).
- `python -X utf8 tests/source_platform_smoke.py`: **OK**.
- `python -X utf8 -m compileall -q src tests scripts`: **OK**.
- `python -X utf8 -m pytest -q tests/test_third_party_licenses.py`: **1 passed**.
- `python -X utf8 scripts/certify_release.py verify`: Hash-/Source-Archiv-Prüfung
  mit 3 Einträgen bestanden, aber Hinweis `MSIX nicht in SHA256SUMS.txt`.
- `... certify_release.py verify --require-msix`: Exit 1,
  `Nicht zertifizierbare Artefakte: PromptBoard.msix`.
- Der `dev`-Projekt-Scan bestätigt Python/pyproject/requirements und Git.

Die lokalen Dokumente halten v1.1.1 als veröffentlichte Artefaktlinie,
116/116 Tests und unveröffentlichte Entwicklungsmetadaten 1.1.3 getrennt;
Store-P1, `makeappx.exe`, Partner-Center-Werte und erhöhter WACK-Readback
bleiben offen. Dieser saubere Klon ist nur Prüf-/Evidenzbasis und kein
Writeback auf die gebundene OneDrive-Projektion.

## Disposition

1136/1137 bleiben **offen / blocked_foreign_state**. Es gibt keinen belastbaren
Grund, den fremden OneDrive-Stand zu adoptieren oder die TASKPLAN-Datensätze
als erledigt zu markieren. Nach diesem Readback wird der exakt gelieferte
stale Selektorpfad einmal übersprungen; anschließend erfolgt genau eine neue
Selektorabfrage. Das persistierte TASKSOLVER-Goal bleibt aktiv.
